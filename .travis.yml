language: java
sudo: false
script: mvn clean verify

jdk:
  - openjdk8

sudo: required
services:
- docker
script:
- docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
- mvn clean package docker:build
- docker push docker.io/jverhulst/movieinfoservice
